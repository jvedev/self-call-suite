{
  "name": "self-call-suite",
  "version": "1.0.0",
  "private": true,
  "description": "Suite of apps for self-call HEMA system",
  "workspaces": [
    "shared/*",
    "apps/*"
  ],
  "scripts": {
    "build:tests": "cd apps/tests && npm run build && cd ../.."
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "jest": "^29.5.0",
    "typescript": "^5.0.0",
    "vite": "^7.1.11"
  },
  "dependencies": {
    "valtio": "^1.11.2"
  }
}
