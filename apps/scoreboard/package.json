{
  "name": "@apps/scoreboard",
  "version": "1.0.0",
  "description": "Scoreboard display application for HEMA matches",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@shared/modules": "*",
    "@shared/styles": "*",
    "@shared/views": "*",
    "@shared/web-components": "*"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vite": "^5.0.0"
  }
}