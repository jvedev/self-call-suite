{
  "name": "@apps/self-call",
  "version": "1.0.0",
  "description": "Self-call HEMA scoring application",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@shared/modules": "workspace:*",
    "@shared/styles": "workspace:*",
    "@shared/views": "workspace:*",
    "@shared/web-components": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vite": "^5.0.0",
    "vite-plugin-pwa": "^0.17.0"
  }
}