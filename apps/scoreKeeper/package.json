{
  "name": "@apps/score-keeper",
  "version": "1.0.0",
  "description": "Alternative scoring interface for table crew",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@shared/modules": "*",
    "@shared/styles": "*",
    "@shared/views": "*",
    "@shared/web-components": "*"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vite": "^5.0.0"
  }
}