# Shared Views

This directory contains reusable view components that can be shared across different apps in the suite.

## Structure

Views should be organized by functionality:
- Authentication views (login, register, forgot password)
- Match-related views (match display, timer components)
- Settings views
- Common UI components

## Usage

Views can be imported and used across different apps to maintain consistency and reduce code duplication.